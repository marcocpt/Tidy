# Tidy 文档治理与目录规范

> 最后更新：2026-07-28 | 版本：v0.5
> 文档状态：草案
> 适用范围：Tidy 仓库根目录的 `docs.md`、`AGENTS.md` 以及 `docs/` 下的规划、架构、阶段规格、标准和历史文档

## 0. 文档定位

本文档是 Tidy 文档体系的入口，位于仓库根目录。只负责：

- 定义根目录与 `docs/` 的目录和命名；
- 规定各类文档的唯一维护位置；
- 规定阶段文档的创建顺序和同步规则；
- 列出当前阶段文档和产物的编写顺序。

本文档不复制 [Tidy 窗口管理 App 功能列表](docs/Tidy%20窗口管理%20App%20功能列表.md)中的功能优先级、模块分解与状态机定义，也不承载具体需求、设计、测试或编码规范。

发生冲突时按以下优先级处理：

```text
Tidy 开发路线图
  ↓
全局架构契约与 ADR
  ↓
阶段需求与验收
  ↓
阶段设计文档
  ↓
阶段测试用例表
  ↓
阶段实现计划
  ↓
阶段产物与历史归档
```

下游文档发现上游不完整或冲突时，必须先修正上游，不得在下游另写一套规则。

## 1. 目标目录

Tidy 按 Phase 聚合文档。目录和文件仅在实际需要时创建，不提前生成空壳。

```text
.                                       # 仓库根目录
├── docs.md                              # 本文件，文档治理与目录规范
├── AGENTS.md                            # AI Agent 执行纪律、任务模板与输出格式
├── .swiftlint.yml                       # SwiftLint 配置（P0 完成时建立）
└── docs/
    ├── Tidy 窗口管理 App 功能列表.md       # 已存在，承载 P0/P1/P2 功能优先级、模块组织、状态机
    ├── Tidy 窗口管理 App 市场调研报告.md    # 已存在，承载市场定位、风险与商业模式
    ├── Tidy_开发路线图.md                  # 计划路径，按需创建
    ├── specs/
    │   └── Tidy_已实现行为规格.md          # 计划路径，按需创建
    ├── architecture/
    │   ├── Tidy_全局架构契约.md
    │   └── adr/
    │       └── ADR-NNNN-主题.md
    ├── phases/
    │   ├── P0_技术探针/                    # 准备阶段（技术验证 + 项目骨架），不参与 F 功能编号
    │   │   ├── P0_01_阶段需求与验收.md
    │   │   ├── P0_02_设计文档.md
    │   │   ├── P0_03_测试用例表.md
    │   │   ├── P0_04_实现计划.md
    │   │   └── artifacts/
    │   ├── F0_权限引导/
    │   │   ├── F0_01_阶段需求与验收.md
    │   │   ├── F0_02_设计文档.md
    │   │   ├── F0_03_测试用例表.md
    │   │   ├── F0_04_实现计划.md
    │   │   └── artifacts/
    │   ├── F0.1_状态栏与快捷键/
    │   ├── F1_窗口编排核心/
    │   ├── F2_应用专属配置/
    │   ├── F3_自定义布局模板/
    │   ├── F4_循环切换/
    │   └── F5_跨App编排/
    ├── standards/
    │   ├── CODING_STANDARDS.md
    │   ├── git-commit-message.md
    │   ├── swift-rules.md
    │   └── xctest-rules.md
    └── historys/
```

目录规则：

- `docs.md` 与 `AGENTS.md` 位于仓库根目录；前者面向人类协作者，后者面向 AI Agent；
- `.swiftlint.yml` 位于根目录，便于 CLI 与 IDE 直接读取；
- `architecture/` 在 P0 形成首份架构契约或 ADR 时创建；
- `P0_技术探针/` 是准备阶段目录，承载技术验证（AX 兼容性、CGEventTap、NSPanel、性能等）与项目骨架，不参与 F 功能编号；
- 阶段目录在该功能的需求合同成为当前 Gate 输入时按需创建；仅创建下一功能的 `F{n}_01_阶段需求与验收.md` 属于阶段准备，不代表下一功能已经开始；
- 功能正式开始后，才允许在对应目录继续创建设计文档、测试用例表和实现计划；
- `artifacts/` 保存清单、矩阵、基线和证据，不新增需求或架构规则；
- `standards/` 必须按 Tidy 实际代码和工具链重新基线后创建；
- `AGENTS.md` 在 AI 工作纪律稳定后创建；`specs/Tidy_已实现行为规格.md` 在首批外部行为获批准时创建，均不提前生成空壳；
- `historys/` 只保存失效草案和历史输入，不作为当前规则来源；
- 现有的两份文档（功能列表、市场调研报告）保留原命名不强制改名，后续新增文档统一加 `Tidy_` 前缀。

## 2. 单一事实来源

| 信息 | 唯一维护位置 |
|---|---|
| 阶段战略目标、边界、依赖、状态、关键路径和路线级 Gate | `docs/Tidy_开发路线图.md` |
| 文档目录、命名、创建顺序和同步规则 | `docs.md` |
| AI Agent 执行纪律、任务模板和输出格式 | `AGENTS.md` |
| 已批准的跨阶段架构不变量 | `docs/architecture/Tidy_全局架构契约.md` |
| 单个架构决策及理由 | `docs/architecture/adr/ADR-NNNN-主题.md` |
| P0 技术探针范围、FR、NFR、兼容性矩阵和验收标准 | `docs/phases/P0_技术探针/P0_01_阶段需求与验收.md` |
| P0 模块职责、数据流、错误和回退 | `docs/phases/P0_技术探针/P0_02_设计文档.md` |
| P0 Acceptance Criteria 覆盖和证据 | `docs/phases/P0_技术探针/P0_03_测试用例表.md` |
| P0 文件、任务、命令和回滚 | `docs/phases/P0_技术探针/P0_04_实现计划.md` |
| P0 清单、兼容性矩阵和阶段证据 | `docs/phases/P0_技术探针/artifacts/` |
| 阶段详细范围、FR、NFR 和可测验收标准 | `docs/phases/F{n}_{简短功能名}/F{n}_01_阶段需求与验收.md` |
| 模块职责、数据流、错误和回退 | `docs/phases/F{n}_{简短功能名}/F{n}_02_设计文档.md` |
| Acceptance Criteria 覆盖和证据 | `docs/phases/F{n}_{简短功能名}/F{n}_03_测试用例表.md` |
| 文件、任务、命令和回滚 | `docs/phases/F{n}_{简短功能名}/F{n}_04_实现计划.md` |
| 清单、矩阵和阶段证据 | `docs/phases/F{n}_{简短功能名}/artifacts/` |
| Swift、XCTest、Git 和完整编码规范 | `docs/standards/` 对应文件 |
| Tidy 已发布、外部可观察行为 | `docs/specs/Tidy_已实现行为规格.md` |
| 被取代但仍需追溯的文档 | `docs/historys/` |
| P0/P1/P2 功能优先级、模块组织、状态机、视觉规格 | `docs/Tidy 窗口管理 App 功能列表.md`（已存在） |
| 市场定位、竞品、风险、商业模式、P1 验证门槛 | `docs/Tidy 窗口管理 App 市场调研报告.md`（已存在） |

同一事实不得在多份文档中复制维护。引用上游时使用编号、表格 ID 或相对链接。

路线图中的 Goal、IN、OUT 和 Exit Gate 定义阶段的战略目标、边界和路线级准入条件。阶段需求中的 Scope 是对路线图 IN 的细化，Out of Scope 是对路线图 OUT 的细化；阶段需求可以在该边界内继续定义 FR、NFR 和 Acceptance Criteria，但不得扩大、缩小或改变路线图边界。需要改变边界时必须先修改路线图。

### 2.1 已有文档的事实归属

| 已有文档 | 唯一负责 | 暂由其承载但应迁移的事实 |
|---|---|---|
| `docs/Tidy 窗口管理 App 功能列表.md` | 功能优先级（P0/P1/P2）、模块组织（M1~M5）、状态机（5 状态）、字母标签视觉规格、技术选型摘要、风险摘要 | 阶段路线 Gate 应迁至 `docs/Tidy_开发路线图.md`；架构决策摘要应迁至 `docs/architecture/Tidy_全局架构契约.md` 与对应 ADR；编码规范细节应迁至 `docs/standards/` |
| `docs/Tidy 窗口管理 App 市场调研报告.md` | 市场定位、竞品、内测验证门槛、商业模式、命名风险 | 无需迁移 |

迁移时机：在 `docs/Tidy_开发路线图.md` 或对应阶段文档正式创建时一次性抽离，不重复维护；迁移前以已有文档为唯一来源。

## 3. 阶段文档

### 3.1 命名与顺序

`docs/phases/` 下有两类目录：

- **准备阶段** `P0_技术探针/`：以 `P` 前缀标识，承载技术验证与项目骨架，不参与功能编号；后续如有新的准备阶段可继续使用 `P{n}_` 前缀；
- **功能阶段** `F{功能编号}_{简短功能名}/`：以 `F` 前缀标识，每个目录对应一个功能。

```text
docs/phases/P0_{准备阶段名}/          # 准备阶段（P 前缀）
docs/phases/F{功能编号}_{简短功能名}/   # 功能阶段（F 前缀）
```

功能编号必须与 [Tidy 窗口管理 App 功能列表](../Tidy%20窗口管理%20App%20功能列表.md)中的 F 编号一致；目录中的简短功能名允许使用稳定缩写（如 `权限引导`、`状态栏与快捷键`、`窗口编排核心`），不要求与功能列表标题逐字一致。阶段目录一经建立，除必要情况不得因功能列表措辞调整而重命名。

> **命名说明**：`docs/phases/` 下的 `P{n}` 指准备阶段，`F{n}` 既指功能编号也指功能开发阶段；`docs/Tidy 窗口管理 App 功能列表.md` 中的 `P0/P1/P2` 指**功能优先级**（Priority），是独立维度。

准备阶段与功能阶段均按固定顺序创建核心文档（`{X}` 为阶段前缀，准备阶段用 `P0`，功能阶段用 `F{n}`）：

```text
{X}_01_阶段需求与验收.md
  ↓
{X}_02_设计文档.md
  ↓
{X}_03_测试用例表.md
  ↓
{X}_04_实现计划.md
  ↓
编码或功能验证
```

涉及用户可见 UI 行为时，在设计之后增加 `{X}_02a_视觉原型.html`；纯窗口枚举、AX 操作、状态机或网格算法等基础设施功能不创建视觉原型。

阶段核心文档命名规则：

- 阶段根目录下的核心文档（`01_阶段需求与验收.md`、`02_设计文档.md`、`03_测试用例表.md`、`04_实现计划.md`）和视觉原型统一加 `{阶段前缀}_` 前缀，准备阶段如 `P0_01_阶段需求与验收.md`，功能阶段如 `F0_01_阶段需求与验收.md`；
- 每份核心文档对应的审查/审核记录（`*_审查记录.md`、`*_审核记录.md`）同样加 `{阶段前缀}_` 前缀，且与正文档同名拼接，例如 `P0_01_阶段需求与验收_审查记录.md`；
- `artifacts/` 子目录下的文件不加阶段前缀。

### 3.2 职责边界

| 文档 | 回答什么 | 不包含什么 |
|---|---|---|
| 需求与验收 | Goal、Scope、Out of Scope、需求、Acceptance Criteria、决策自由度 | 类名、方法、字段、文件和实现步骤 |
| 设计文档 | 模块职责、依赖、数据流、状态、错误和回退 | 完整实现代码和逐文件任务 |
| 测试用例表 | AC 对应的测试 ID、覆盖状态、证据和环境 | 新需求和未经批准的产品语义 |
| 实现计划 | 文件、任务、测试、命令、提交边界和回滚 | 未批准阶段和未来抽象 |
| Artifact | 已执行工作的清单、矩阵、快照和证据 | 新需求、阶段状态和架构规则 |

测试用例表必须：

- 标记 `COVERED`、`PARTIAL`、`MISSING` 或 `DEFERRED`；
- 区分 Unit、Characterization、Corpus、Golden、Compatibility、Performance、Fuzz 和 UI 证据；
- 区分观测行为与批准的产品语义；
- 对 `KNOWN_DEFECT`、`TOLERATED_COMPATIBILITY` 和 `REVIEW` 明确处置；
- 不把单个兼容性测试输出当作绝对真值。

## 4. 阶段推进顺序

Tidy 先经准备阶段（P0_技术探针）完成技术验证与项目骨架，再按功能优先级与依赖关系推进开发，每个功能即一个开发阶段：

```text
P0_技术探针（准备阶段：AX/CGEventTap/NSPanel/性能验证 + 项目骨架）
  ↓ Exit Gate：技术可行性已证明，骨架可编译
F0_权限引导（最低风险，权限基础）
  ↓
F0.1_状态栏与快捷键（Carbon 热键 + NSStatusItem）
  ↓
F1_窗口编排核心（AX + CGEventTap + NSPanel + 网格 + 状态机）
  ↓
F2_应用专属配置（P1 优先级）
  ↓
F3_自定义布局模板（P1 优先级）
  ↓
F4_循环切换（P2 优先级）
  ↓
F5_跨App编排（P2 优先级）
```

### 4.1 单阶段推进流程

准备阶段与功能阶段均按以下顺序创建文档与产物（`{X}` 为阶段前缀，准备阶段 `P0`，功能阶段 `F{n}`）：

1. `docs/phases/{X}_{简短名}/{X}_01_阶段需求与验收.md`；
2. `docs/phases/{X}_{简短名}/{X}_02_设计文档.md`；
3. `docs/phases/{X}_{简短名}/{X}_03_测试用例表.md`；
4. `docs/phases/{X}_{简短名}/{X}_04_实现计划.md`；
5. 编码实现与功能验证；
6. 按需生成 artifacts（兼容性矩阵、性能数据、证据等）。

### 4.2 架构与 ADR 时机

- `docs/architecture/Tidy_全局架构契约.md` 在 P0 完成首批验证后编写雏形，F1 验证后冻结；
- 首批 ADR（激活模型、窗口范围、目标屏幕、还原语义、EventTap 生命周期）在 P0 期间探索、F1 期间验证后冻结；
- 后续功能（F2~F5）按需产出新 ADR。

### 4.3 阶段间依赖规则

- P0 Exit Gate 通过后，才允许创建 `F0_01_阶段需求与验收.md`；
- 当前功能 `{X}_01_阶段需求与验收.md` 批准后，才允许开始 `{X}` 的编码实现；
- 当前功能 Exit Gate 通过后，才允许创建下一功能 `{X+1}_01_阶段需求与验收.md`；
- 下一功能的需求文档在当前功能期间只能处于草案状态；创建或批准该文档本身不代表下一功能已经开始。

### 4.4 技术风险验证（P0_技术探针）

P0_技术探针 是准备阶段，集中承载以下技术验证与项目骨架，不参与 F 功能编号：

| 验证项 | 类别 | 说明 |
|---|---|---|
| AX 权限检测 | 权限 | AX 授权状态检测与监听 |
| AX 窗口枚举 | 枚举 | CGWindowList + AX 匹配 |
| AXSetFrame 兼容性 | 操作 | 多 App 成功率与重试 |
| AX 聚焦与 kAXFocusedWindowAttribute | 操作 | 焦点窗口获取 |
| Carbon 热键注册 | 输入 | RegisterEventHotKey 验证 |
| CGEventTap 生命周期 | 输入 | 创建/启用/禁用/恢复 |
| NSPanel 覆盖层 | UI | 层级/跨 Space/鼠标穿透 |
| NSStatusItem | UI | 状态栏图标与菜单 |
| 网格布局算法 | 算法 | 自适应网格性能 |
| 多显示器坐标 | 显示 | 双屏/三屏场景 |
| 排列完成时间 | 性能 | 2/5/9 窗口 P95 |
| SPM 三 target 骨架 | 工程 | TidyCore / TidyUI / TidyApp 可编译 |
| SwiftLint 配置 | 工程 | `.swiftlint.yml` 建立并通过 `--strict` |

P0 的需求与设计文档应包含完整的兼容性验证矩阵与性能门槛（参考市场调研报告第七节）。P0 Exit Gate 通过后，F1 可直接基于已验证的组件进入编排核心实现，无需重复技术探针。

## 5. Standards 后续规则

参考项目的"短工具入口 → 详细权威规范 → 自动验证"分层可以复用，但具体规则不能照搬。

Tidy 当前事实包括：

- 目标平台为 macOS 12（Monterey）及以上；
- 开发工具为 Xcode 14.1+ / Swift 5.7+；
- 采用 SPM 三 target 结构：`TidyCore`（纯 Swift + 系统框架，无 AppKit）、`TidyUI`（AppKit 覆盖层 + SwiftUI 偏好窗口 + TidyCore）、`TidyApp`（应用入口 + TidyCore + TidyUI）；
- 偏好窗口使用 `NSWindow + SwiftUI content`，覆盖层坚持 AppKit；
- 状态栏图标 4 种状态（普通 / ⚠️ 未授权 / 🔵 编排中 / 🔒 锁定工作）；
- 默认热键 ⌘⌥T，通过 Carbon `RegisterEventHotKey` 注册；
- selecting 阶段通过 `CGEventTap`（`.cgSessionEventTap + .defaultTap + .headInsertEventTap`）拦截键盘；
- 窗口操作通过 `AXUIElement` 公开 API（`kAXPositionAttribute` + `kAXSizeAttribute` + `kAXFocusedWindowAttribute` + `kAXWindowsAttribute`）；
- 日志使用 `os_log`（`com.tidy.windowmanagement` 子系统）；
- 测试采用 XCTest + XCUITest；
- Tidy 的 SwiftLint 配置、CI 流水线和 macOS 兼容性矩阵仍需在 P0 中确认。

因此本轮不创建 `docs/standards/`。后续分别审核后编写 4 份文件：

1. `docs/standards/CODING_STANDARDS.md`：标准入口、跨主题原则和详细规范索引；
2. `docs/standards/swift-rules.md`：Swift 规则的唯一来源，包含命名、并发模型、Combine 使用约定；
3. `docs/standards/xctest-rules.md`：XCTest / XCUITest 平台矩阵和测试纪律的唯一来源；
4. `docs/standards/git-commit-message.md`：提交格式和 scope 的唯一来源。

工具专用规则只保留阅读入口和不可省略的硬约束，不复制完整标准。

### 5.1 SwiftLint 硬约束

**Tidy 是 Swift 项目，任何 Git 提交前必须运行 SwiftLint 检查，并修复所有 error 级违规。**

- 提交前命令：`swiftlint lint --strict`（在仓库根目录执行，配置文件 `.swiftlint.yml` 在 P0 完成时建立）；
- `warning` 级违规应在同一提交内修复，确实无法修复时必须在 commit message 中显式说明豁免理由；
- CI 流水线必须将 SwiftLint 设为强制门禁；
- 第三方依赖与生成代码允许通过 `excluded` 排除。

## 6. 通用编写规则

### 6.1 元数据和版本

Markdown 标题后必须包含：

```text
> 最后更新：YYYY-MM-DD | 版本：vX.Y 或 vX.Y.Z
> 文档状态：草案 / 审核中 / 已批准 / 已实现 / 已取代 / 归档
```

- 内容、范围、接口或验收标准变化时更新版本；
- 文档末尾维护列表格式版本记录；
- 文档状态不得替代路线图中的阶段状态；
- 当前权威文件名不加 `final`、`new`、`最新版` 或版本号；
- 只有历史归档文件保留原始版本后缀。

### 6.2 链接、语言和图表

- 仓库内使用相对链接，不硬编码个人电脑绝对路径；
- 根目录的 `docs.md`、`AGENTS.md` 引用 `docs/` 下文件时使用 `docs/...` 相对路径；
- 跨仓库内容（如 Macim 设计参考）使用"仓库名 + 仓库内路径"描述；
- 不创建指向尚不存在文件的 Markdown 链接，规划路径使用代码格式；
- 中文正文使用中文标点，英文术语保持原文；
- Mermaid 用于流程、状态、时序和依赖图；
- Mermaid 节点含空格、括号或标点时使用引号；
- 图表必须有文字解释，不能成为唯一规范来源；
- 用户决策问题必须使用 `AskUserQuestion` 工具给出 2~4 个结构化选项，不得用纯文本提问中断会话。

### 6.3 同步规则

| 变更来源 | 必须同步检查 |
|---|---|
| 路线图阶段、依赖或 Gate | 当前阶段需求和受影响链接 |
| 需求 Goal、IN、OUT 或 AC | 设计、测试用例表和实现计划 |
| 设计职责、数据流、接口或回退 | 测试用例表、实现计划、架构契约和 ADR |
| UI 可见行为（字母标签、覆盖层、菜单栏图标） | 视觉原型、测试用例表和已实现行为规格 |
| 测试或覆盖状态 | 测试用例表和阶段 artifacts |
| AX 兼容性矩阵或兼容 App 黑名单 | 测试用例表、AX 失败降级设计和风险章节 |
| 已批准架构不变量或待冻结架构主题 | 全局架构契约、ADR、路线图和受影响设计 |
| P1 验证结果 | 路线图阶段、商业模式判断和后续阶段范围 |

修改后必须检查：

1. 是否产生两个状态来源；
2. 上下游是否矛盾；
3. 相对链接是否有效；
4. 版本和版本记录是否更新；
5. 测试覆盖状态是否真实；
6. 是否把 legacy bug 或兼容性缺陷误写成批准契约。

## 7. History、ADR 与 Git

| 机制 | 记录内容 |
|---|---|
| 文档版本记录 | 本文档各版本改了什么 |
| ADR | 为什么选择或改变跨阶段架构决定 |
| Git commit | 哪些文件在何时发生了变化 |
| `docs/historys/` | 已失效但仍需追溯的完整历史输入 |

Tidy 不要求每个文档 commit 再创建一份重复 history 日志。以下主题变化时必须写 ADR：

- 依赖方向（TidyCore / TidyUI / TidyApp 三 target 划分）；
- 激活模型（单热键 Toggle vs 多激活方式）；
- 窗口范围（当前 Space 可见 vs 跨 Space 聚合）；
- 目标屏幕判定策略；
- 还原语义（撤销排列 vs 追踪 working 阶段手动操作）；
- CGEventTap 生命周期（仅 selecting 启用 vs 全程常驻）；
- AXSetFrame 失败处理（跳过 + >50% 终止 vs 原位置标签降级）；
- 偏好窗口技术选型（NSWindow + SwiftUI vs 纯 AppKit）；
- 字母标签位置（左上角 vs 中心偏上动态约束）；
- P1 验证后的默认作用域调整（如多屏聚合策略）。

文档提交使用 Conventional Commits：

```text
docs(<scope>): <简洁祈使语气主题>
```

`<scope>` 可选：`governance`、`roadmap`、`architecture`、`adr`、`phase-P{n}`、`phase-F{n}`、`standards`、`spec`、`history`、`agents`。一个提交只处理一个可独立审查的文档主题。详细规则以后由 `docs/standards/git-commit-message.md` 维护。

## 8. 阅读顺序

AI Agent 开始任务前：

1. 先读 `docs/Tidy_开发路线图.md`（如已创建），确认当前阶段和范围；未创建时以 `docs/Tidy 窗口管理 App 市场调研报告.md` 第十一节阶段路线图为入口；
2. 创建或修改文档时再读根目录 `docs.md`；
3. 执行编码或调试任务时读根目录 `AGENTS.md`，遵守其中的任务模板和输出格式；
4. 按需求、设计、测试用例表、实现计划的顺序读取当前阶段文档；
5. 涉及功能优先级、模块组织或状态机细节时读取 `docs/Tidy 窗口管理 App 功能列表.md`；
6. 涉及市场定位、竞品、风险或商业模式时读取 `docs/Tidy 窗口管理 App 市场调研报告.md`；
7. 只读取与任务直接相关的 artifact、代码和测试；
8. 涉及已批准架构不变量或待冻结架构主题时读取全局架构契约和相关 ADR；
9. 上游文档缺失或未批准时停止编码，先补齐上游；
10. 不以历史归档、旧草案或单个测试现状覆盖当前批准文档；
11. 涉及"浏览器中展示"的需求直接执行，不询问用户。

## 9. 版本记录

- **v0.5（2026-07-28）**
  - 恢复 `P0_技术探针/` 作为准备阶段目录，与 `F0_` 及以后功能阶段共存：`phases/` 下先有 `P0_技术探针/`，再有序排列 `F0_权限引导/`、`F0.1_`、`F1_` …；
  - 第 3.1 节重写命名规则：区分 `P{n}_` 准备阶段前缀与 `F{n}_` 功能阶段前缀，核心文档统一用 `{X}_` 前缀模板；
  - 第 4 节改名为"阶段推进顺序"，新增 `P0_技术探针 → F0` 的 Exit Gate 衔接；第 4.4 节将 AX、CGEventTap、NSPanel、性能、SPM 骨架、SwiftLint 配置等技术验证回归 P0 集中承载，不再分散到 F0/F1；
  - 第 4.2 节架构与 ADR 时机改为 P0 期间探索、F1 期间冻结；
  - 第 2 节单一事实来源表补充 P0_技术探针 5 行（需求、设计、测试、实现计划、artifacts）；
  - 第 1 节目录树补回 `P0_技术探针/` 子树；第 5 节 SwiftLint 与兼容性矩阵的确认时机改回 P0；第 7 节 `<scope>` 同时支持 `phase-P{n}` 与 `phase-F{n}`。
- **v0.4（2026-07-28）**
  - 按"功能即开发阶段"重新划分：阶段目录编号从 `P{n}_` 改为 `F{n}_` 前缀的功能编号，与 [Tidy 窗口管理 App 功能列表](../Tidy%20窗口管理%20App%20功能列表.md) 中的 F 编号对齐；
  - 重设功能推进顺序为：`F0_权限引导` → `F0.1_状态栏与快捷键` → `F1_窗口编排核心` → `F2_应用专属配置` → `F3_自定义布局模板` → `F4_循环切换` → `F5_跨App编排`（第 4 节）；
  - 取消独立的 `P0_技术探针` 阶段目录：原技术验证（AX 兼容性、CGEventTap、NSPanel、网格算法、多显示器、性能等）按归属功能分散到第 4.4 节技术风险验证矩阵；
  - 删除 `docs/phases/P0_技术探针/` 目录及其下 `P0_01_阶段需求与验收.md`、`P0_02_设计文档.md` 两份草案；
  - 同步更新目录树（第 1 节）、单一事实来源表（第 2 节）、阶段文档命名规则（第 3.1 节）、ADR 时机（第 4.2 节）和 SwiftLint 配置建立时机（第 5.1 节 `F0 完成时建立`）；
  - 第 3.1 节更新"命名说明"：`docs/phases/` 下的 `F{n}` 既指功能编号也指开发阶段，与功能列表.md 中的 `P0/P1/P2` 功能优先级是独立维度。
- **v0.3（2026-07-28）**
  - 阶段编号统一为 `P0~P4`，消除原 `P-1` 与 `P0` 的命名不一致；
  - 移除阶段目录名中的版本号前缀（`v0.1`、`v1.0`、`v1.x`、`v2.0`），改用语义化短名：`P0_技术探针` / `P1_内测验证` / `P2_MVP核心闭环` / `P3_增强` / `P4_高级功能`；
  - 新增"命名空间说明"，显式区分**阶段编号 P{n}**（本文档与 `docs/phases/`）与**功能优先级 P{n}**（功能列表.md）两个独立维度；
  - 同步更新第 4 节阶段文档顺序、第 5 节 SwiftLint 配置时机、第 6 节同步规则、第 7 节 ADR 主题清单中的阶段引用。
- **v0.2（2026-07-28）**
  - 将 `docs.md` 从 `docs/` 子目录移至仓库根目录，统一作为项目入口；
  - 用根目录 `AGENTS.md` 替代原计划的 `docs/Tidy_AI执行规则.md`，对齐 Codex / Cursor / Claude Code 行业标准；
  - 补充根目录 `.swiftlint.yml` 配置位置说明；
  - 更新单一事实来源表、目录树、阅读顺序和同步规则中的路径引用；
  - `git-commit-message.md` 的 `<scope>` 增加 `agents` 取值。
- **v0.1（2026-07-28）**
  - 建立 Tidy 文档治理入口；
  - 采用按 Phase 聚合的目录结构，与市场调研报告阶段路线图对齐；
  - 承认并归位现有两份文档（功能列表、市场调研报告）的事实归属，列出待迁移事实；
  - 定义单一事实来源、阶段文档职责和同步规则；
  - 明确 P0 下一步文档队列与首批 ADR 主题；
  - 引入 SwiftLint 提交前硬约束，符合 Swift 项目规范；
  - 引入 AskUserQuestion 会话交互规范；
  - 保留参考项目（CPDF）的分层治理思想，但不复制不符合 Tidy 基线的 PDF 引擎、C ABI、Reference Lab 等假设；
  - 区分版本记录、ADR、Git commit 和 `historys/` 的职责。
