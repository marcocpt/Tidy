# ADR-0005：输入拦截仅选择阶段启用

- **状态**：已批准
- **关联不变量**：INV-007

## 上下文（Context）

Tidy 需要在选择阶段拦截键盘输入以实现字母选择。输入拦截是系统级能力，如果全程启用会影响用户正常工作。

## 决策（Decision）

键盘事件拦截功能只能在编排的选择阶段启用，其他阶段必须完全禁用。拦截功能不得影响正常的应用操作。拦截的生命周期必须与状态机严格绑定。

## 影响（Consequences）

- 用户在工作阶段不受拦截影响
- 状态机转换必须精确控制拦截的启用和禁用
- 拦截启用/禁用的时序必须是原子操作
