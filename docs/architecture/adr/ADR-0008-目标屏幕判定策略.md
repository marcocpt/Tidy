# ADR-0008：目标屏幕判定策略

- **状态**：候选（待评审）
- **关联不变量**：INV-006

## 上下文（Context）

多显示器场景下，Tidy 需要确定编排窗口的目标屏幕。是将所有窗口聚合到焦点屏幕，还是在各自所在屏幕上排列？这影响布局算法和用户预期。

## 决策（Decision）

待评审。初步倾向：v0.1 默认在焦点所在屏幕排列，后续根据 v0.1 Pilot 验证结果决定是否支持跨屏聚合。

## 影响（Consequences）

待评审确认后补全。
