# ADR-0006：激活模型

- **状态**：候选（待评审）
- **关联不变量**：INV-006, INV-007

## 上下文（Context）

Tidy 需要确定用户如何触发编排流程。当前默认方案是单一热键切换，但未来可能需要多激活方式（如多种热键组合、手势或菜单触发）。激活模型影响状态机的入口设计和输入拦截的启用时机。

## 决策（Decision）

待评审。初步倾向：v0.1 使用单一热键切换模式，后续根据用户反馈决定是否支持多激活方式。

## 影响（Consequences）

待评审确认后补全。
