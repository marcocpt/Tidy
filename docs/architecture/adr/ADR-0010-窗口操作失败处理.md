# ADR-0010：窗口操作失败处理

- **状态**：候选（待评审）
- **关联不变量**：INV-005, INV-009

## 上下文（Context）

部分应用可能不响应辅助功能的窗口操作请求。Tidy 需要确定失败时的处理策略：是跳过失败窗口继续编排、还是在失败率达到阈值时终止编排。

## 决策（Decision）

待评审。初步倾向：单窗口失败时保留原位置并显示原位置标签降级，失败率超过 50% 时终止编排并还原已操作的窗口。

## 影响（Consequences）

待评审确认后补全。
