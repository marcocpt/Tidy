# ADR-0007：窗口范围

- **状态**：候选（待评审）
- **关联不变量**：INV-006

## 上下文（Context）

Tidy 需要确定编排时操作哪些窗口。当前默认方案是当前空间中前台应用的可见窗口，但未来可能需要跨空间聚合。窗口范围影响枚举逻辑和用户心智模型。

## 决策（Decision）

待评审。初步倾向：v0.1 限定为当前空间、前台应用、可见窗口，后续根据 v0.1 Pilot 验证结果决定是否扩展。

## 影响（Consequences）

待评审确认后补全。
