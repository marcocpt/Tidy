# ADR-0002：覆盖层坚持原生窗口框架

- **状态**：已批准
- **关联不变量**：INV-003

## 上下文（Context）

Tidy 的覆盖层需要在系统窗口层级中精确控制层级、跨空间显示和鼠标穿透行为。声明式 UI 框架与系统窗口面板的集成存在已知兼容性问题。

## 决策（Decision）

窗口覆盖层（字母标签、编排反馈）必须使用原生窗口框架实现，不得使用声明式 UI 框架。

## 影响（Consequences）

- 覆盖层行为在目标平台上可靠且可预测
- 覆盖层代码量可能比声明式方案更多
- 偏好窗口不受此约束，可使用声明式框架
